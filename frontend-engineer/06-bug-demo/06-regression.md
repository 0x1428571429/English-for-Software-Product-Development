### "The cache was the issue all along"
查了半天发现是缓存问题。
还可以说：
- Turns out it was just cached data
- A hard refresh fixed it
- Clear cache and it works
- We should have tried that first

### "It's a regression — this was fixed before"
以前修过的 bug 又回来了。
还可以说：
- This was fixed in the last release
- Someone re-introduced the bug
- We need a regression test for this
- The fix was lost in a merge
