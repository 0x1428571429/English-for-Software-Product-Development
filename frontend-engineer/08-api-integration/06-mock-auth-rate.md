### "The mock data format is wrong"
AI 生成的 mock 数据格式不对。
还可以说：
- AI generated the wrong response structure
- The mock doesn't match the API spec
- I had to fix AI's mock data
- AI doesn't know our API format

### "I got rate limited while testing"
接口有 rate limit，测一半被 ban 了。
还可以说：
- The API blocked me after too many requests
- I hit the rate limit
- Can you increase the limit for testing?
- I need a higher rate limit for dev

### "The API returns 200 but the data is wrong"
返回 200 但数据错了。
还可以说：
- The status code is 200 but the response is wrong
- No error but incorrect data
- We need better error handling
- 200 doesn't mean it's correct
