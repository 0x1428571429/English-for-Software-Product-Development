### "QA said it's all tested but it broke in production"
测试通过了，上线就炸。
还可以说：
- It passed QA but failed in production
- The test environment didn't catch it
- We missed a production-only scenario
- The data was different in production

### 回归测试太多
还可以说：
- Regression suite is huge
- We can't run all tests before every deploy
- Need to prioritize regression tests
- The build takes too long due to tests
